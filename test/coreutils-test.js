var expect = require("chai").expect;

describe("coreutils.js", function() {
  describe("module.exports.log", function() {
    it("string format", function() {
      expect(1).to.equal(0);
    });
  });

  describe("module.exports.getCommand", function() {
    it("identifies the correct format", function() {
      expect(1).to.equal(0);
    });

    it("returns the proper arguments", function() {
      expect(1).to.equal(0);
    });
  });

  describe("module.exports.isAdministrator", function() {
    it("identifies admins", function() {
      expect(1).to.equal(0);
    });
  });

  describe("module.exports.addHelpText", function() {
    it("adds help text", function() {
      expect(1).to.equal(0);
    });
  });

  describe("module.exports.readJSONData", function() {
    it("reads JSON files", function() {
      expect(1).to.equal(0);
    });
  });

  describe("module.exports.writeJSONData", function() {
    it("writes JSON files", function() {
      expect(1).to.equal(0);
    });
  });
});