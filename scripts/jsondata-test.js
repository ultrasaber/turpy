/*var testObject = {
    foo: "bar",
    bar: "foo"
}

turpy.writeJSONData('data/global', 'json-test', testObject, () => {
    turpy.log('[TEST] Successully wrote data.');

    turpy.readJSONData('data/global', 'json-test', (readObject) => {
        turpy.log(util.inspect(readObject));
    });
});*/
